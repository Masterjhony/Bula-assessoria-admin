---
phase: 02-envasamento-bula-bulinho
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Footer.tsx
autonomous: true
requirements:
  - CONV-05
  - COPY-03
must_haves:
  truths:
    - "O eyebrow da CTA strip exibe 'ASSESSORIA DE QUEM VAI NA PONTA'"
    - "O H3 exibe duas linhas: 'Você compra com confiança.' e 'A gente já esteve no curral.'"
    - "A subline exibe o texto do Bulinho com menção ao frete grátis"
    - "O botão CTA exibe 'Quero a assessoria da Bula'"
  artifacts:
    - path: src/components/Footer.tsx
      provides: "Footer CTA strip com copy expandido — Bulinho proof + subline"
      contains: "ASSESSORIA DE QUEM VAI NA PONTA"
  key_links:
    - from: src/components/Footer.tsx
      to: "#inscricao-form"
      via: "scrollToForm onClick"
      pattern: "scrollToForm"
---

<objective>
Atualizar o CTA strip do Footer com o copy aprovado de "assessoria de quem vai na ponta", incluindo eyebrow novo, H3 em duas linhas, subline de prova social do Bulinho e botão renomeado.

Purpose: O footer é a última âncora de conversão antes do usuário sair. O copy atual ("Assessoria gratuita nos maiores leilões") não diferencia e não menciona a prova social do apartamento. O novo copy cria urgência com credencial de presença física.

Output: `src/components/Footer.tsx` com CTA strip expandido. Layout existente (flex-col / lg:flex-row) é mantido; apenas os textos são substituídos e uma subline `<p>` é adicionada abaixo do H3.
</objective>

<execution_context>
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/.planning/ROADMAP.md
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir copy do CTA strip no Footer</name>
  <files>src/components/Footer.tsx</files>
  <read_first>
    - src/components/Footer.tsx — ler o bloco "Top CTA strip" (linhas 12–32) para entender a estrutura `div > div > div.max-w-xl + a`. Não alterar a estrutura de layout, classes de flex, padding, ou o botão anchor `a`.
  </read_first>
  <action>
    Dentro do bloco "Top CTA strip" (div com `className="relative overflow-hidden border-t border-white/10"`), localizar o `div.max-w-xl` e aplicar estas substituições exatas:

    1. Eyebrow `<p>`: trocar o conteúdo de `"Bula Assessoria Pecuária"` por `"ASSESSORIA DE QUEM VAI NA PONTA"`. Manter todas as classes existentes: `text-[11px] font-black uppercase tracking-[0.18em] text-white/50`.

    2. H3: trocar o texto de `"Assessoria gratuita nos maiores leilões do Brasil, com frete grátis de Campo Grande até a sua porteira."` por duas linhas separadas com `<br />`:
       ```
       Você compra com confiança.<br />A gente já esteve no curral.
       ```
       Manter todas as classes do H3: `mt-3 text-2xl font-black leading-[1.1] tracking-tight sm:text-3xl`.

    3. Adicionar imediatamente após o H3 (dentro do `div.max-w-xl`, antes do fechamento `</div>`), um novo parágrafo:
       `<p className="mt-3 text-sm leading-relaxed text-white/55">O Bulinho apartou pessoalmente os animais do JMP — frete grátis de Campo Grande até a sua porteira incluso.</p>`

    4. Botão `<a>`: trocar o texto de `"Garantir minha assessoria"` por `"Quero a assessoria da Bula"`. Manter o ícone `MessageCircle` antes e `ArrowRight` depois, manter todas as classes.

    Não alterar: imports, `scrollToForm`, estrutura de layout do footer, colunas do main, bottom bar.
  </action>
  <verify>
    <automated>grep -c "ASSESSORIA DE QUEM VAI NA PONTA" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Footer.tsx && grep -c "Você compra com confiança" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Footer.tsx && grep -c "A gente já esteve no curral" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Footer.tsx && grep -c "Quero a assessoria da Bula" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Footer.tsx && grep -c "O Bulinho apartou pessoalmente" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Footer.tsx</automated>
  </verify>
  <done>
    - Eyebrow lê "ASSESSORIA DE QUEM VAI NA PONTA"
    - H3 contém "Você compra com confiança." e "A gente já esteve no curral."
    - Subline `<p>` contém "O Bulinho apartou pessoalmente os animais do JMP"
    - Botão CTA lê "Quero a assessoria da Bula"
    - `npx tsc --noEmit` sem erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Alterações de copy puro — sem inputs de usuário, sem chamadas de API, sem dados sensíveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
- `grep "ASSESSORIA DE QUEM VAI NA PONTA" src/components/Footer.tsx` retorna 1 match
- `grep "Quero a assessoria da Bula" src/components/Footer.tsx` retorna 1 match
- `grep "O Bulinho apartou pessoalmente" src/components/Footer.tsx` retorna 1 match
- `npx tsc --noEmit` sem erros de TypeScript
</verification>

<success_criteria>
O CTA strip do Footer exibe o copy expandido com eyebrow "ASSESSORIA DE QUEM VAI NA PONTA", H3 em duas linhas, subline com prova social do Bulinho, e botão "Quero a assessoria da Bula".
</success_criteria>

<output>
Criar `.planning/phases/02-envasamento-bula-bulinho/02-01-SUMMARY.md` ao concluir.
</output>
