---
phase: 02-envasamento-bula-bulinho
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ApartamentoGallery.tsx
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - COPY-01
must_haves:
  truths:
    - "O eyebrow exibe 'Acesso exclusivo · Bulinho esteve lá'"
    - "O H2 exibe 'O Bulinho foi lá. Apartou um por um.'"
    - "O parágrafo menciona 'A assessoria da Bula não acontece da varanda'"
    - "Todas as 8 fotos têm captions individuais com o texto aprovado"
    - "Existe um CTA após a galeria com o texto 'Quer saber quais animais o Bulinho selecionou para você?'"
    - "O CTA rola suavemente até #inscricao-form"
  artifacts:
    - path: src/components/ApartamentoGallery.tsx
      provides: "Galeria com heading Bulinho + captions por foto + CTA de conversão"
      contains: "O Bulinho foi lá. Apartou um por um."
  key_links:
    - from: src/components/ApartamentoGallery.tsx
      to: "#inscricao-form"
      via: "scrollIntoView onClick"
      pattern: "inscricao-form"
---

<objective>
Atualizar ApartamentoGallery.tsx com o copy aprovado: novo eyebrow, H2, parágrafo de prova social, captions individuais para cada foto, e um CTA de conversão após a grade de fotos que ancora ao formulário.

Purpose: A galeria é o ponto de prova máxima — onde o usuário vê o Bulinho trabalhando. Sem captions, as fotos são anônimas. Com o copy certo e um CTA imediato após a prova, a galeria vira a maior máquina de conversão da página.

Output: `src/components/ApartamentoGallery.tsx` com heading atualizado, array FOTOS com 8 captions preenchidos, e bloco CTA renderizado abaixo da grade desktop e do carrossel mobile.
</objective>

<execution_context>
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/.planning/ROADMAP.md
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/ApartamentoGallery.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar heading e preencher captions do array FOTOS</name>
  <files>src/components/ApartamentoGallery.tsx</files>
  <read_first>
    - src/components/ApartamentoGallery.tsx — ler as linhas 1–53 (interface GaleriaFoto, array FOTOS, e componente FotoCard). O caption já está wired no hover overlay (linha 44–48); só falta preencher os valores no array.
  </read_first>
  <action>
    1. ARRAY FOTOS — substituir os 8 objetos do array com captions preenchidos. Manter os campos `src`, `alt` e `objectPosition` existentes; apenas adicionar `caption`:

    ```
    { src: '/apartamento/IMG_0003.jpg', alt: 'Apartamento do gado JMP', caption: 'Chegando ao apartamento do JMP' },
    { src: '/apartamento/IMG_0006.jpg', alt: 'Apartamento do gado JMP', caption: 'Bulinho avaliando os animais de perto' },
    { src: '/apartamento/IMG_0037.jpg', alt: 'Apartamento do gado JMP', caption: 'Seleção de conformação no curral' },
    { src: '/apartamento/IMG_0059.jpg', alt: 'Apartamento do gado JMP', caption: 'Verificando genética e estrutura' },
    { src: '/apartamento/IMG_0062.jpg', alt: 'Apartamento do gado JMP', caption: 'Lote selecionado para o 13º JMP' },
    { src: '/apartamento/IMG_0106.jpg', alt: 'Apartamento do gado JMP', caption: 'Bulinho no campo — assessoria presencial' },
    { src: '/apartamento/IMG_0109.jpg', alt: 'Apartamento do gado JMP', caption: 'Cada touro avaliado individualmente' },
    { src: '/apartamento/IMG_0117.jpg', alt: 'Apartamento do gado JMP', objectPosition: 'top', caption: 'Resultado do apartamento — animais de elite' },
    ```

    2. HEADING BLOCK — dentro do `div.mb-10.px-5` (linhas 103–113), substituir:
    - `<span>` eyebrow: trocar `"Prova social · Expertise em campo"` por `"Acesso exclusivo · Bulinho esteve lá"` (manter classes existentes).
    - `<h2>`: trocar `"O Apartamento foi feito pela Bula"` por `"O Bulinho foi lá. Apartou um por um."` (manter classes existentes).
    - `<p>`: trocar `"Nossa equipe esteve presente no apartamento do gado. Veja como foi."` por `"A assessoria da Bula não acontece da varanda — acontece no curral. O Bulinho esteve pessoalmente no apartamento do 13º JMP, olho no olho com cada animal, antes de você gastar um centavo."` (manter classes existentes).

    Não alterar: lightbox, carrossel mobile, dots, grid desktop, a estrutura `FotoCard`.
  </action>
  <verify>
    <automated>grep -c "Acesso exclusivo · Bulinho esteve lá" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/ApartamentoGallery.tsx && grep -c "O Bulinho foi lá. Apartou um por um." /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/ApartamentoGallery.tsx && grep -c "A assessoria da Bula não acontece da varanda" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/ApartamentoGallery.tsx && grep -c "caption:" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/ApartamentoGallery.tsx</automated>
  </verify>
  <done>
    - Eyebrow contém "Acesso exclusivo · Bulinho esteve lá"
    - H2 contém "O Bulinho foi lá. Apartou um por um."
    - Parágrafo contém "A assessoria da Bula não acontece da varanda"
    - O arquivo contém exatamente 8 entradas com `caption:` preenchido
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar bloco CTA de conversão após a galeria</name>
  <files>src/components/ApartamentoGallery.tsx</files>
  <read_first>
    - src/components/ApartamentoGallery.tsx — ler as linhas 98–170 (estrutura do `return`) para identificar onde a seção `FOTOS.length > 0` fecha, antes do fechamento do `div.mx-auto.max-w-7xl`.
  </read_first>
  <action>
    Adicionar uma função auxiliar `scrollToForm` no topo do arquivo (antes do componente `ApartamentoGallery`, após a interface e o array FOTOS). Usar o mesmo padrão de Footer.tsx e Form.tsx:

    `function scrollToForm(e: React.MouseEvent) { e.preventDefault(); document.getElementById('inscricao-form')?.scrollIntoView({ behavior: 'smooth' }) }`

    Dentro do bloco `{FOTOS.length > 0 && ( <> ... </> )}`, após o grid desktop (`div.hidden.sm:grid`) e antes do `</>` de fechamento do Fragment, adicionar o bloco CTA:

    ```jsx
    {/* CTA pós-galeria */}
    <div className="mt-10 px-5 sm:px-8 text-center">
      <p className="text-sm text-white/50 mb-4">
        Quer saber quais animais o Bulinho selecionou para você?
      </p>
      <a
        href="#inscricao"
        onClick={scrollToForm}
        className="inline-flex items-center justify-center gap-2 rounded-xl bg-white px-6 py-3.5 text-sm font-black shadow-sm transition-all hover:-translate-y-0.5 hover:bg-white/90"
        style={{ color: '#111111' }}
      >
        Garantir minha assessoria
      </a>
    </div>
    ```

    O CTA fica visível tanto em mobile (após os dots) quanto em desktop (após o grid). Para mobile: o bloco deve também aparecer após os dots — como ele está dentro do mesmo Fragment `<>`, ele renderizará após ambos. Confirmar que a estrutura do Fragment engloba tanto o carrossel/dots quanto o grid.

    Não alterar: imports existentes (não há necessidade de novo import), lightbox, lógica de estado.
  </action>
  <verify>
    <automated>grep -c "Quer saber quais animais o Bulinho selecionou" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/ApartamentoGallery.tsx && grep -c "inscricao-form" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/ApartamentoGallery.tsx && npx tsc --noEmit --project /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/tsconfig.json 2>&1 | grep -c "error TS" || true</automated>
  </verify>
  <done>
    - Arquivo contém "Quer saber quais animais o Bulinho selecionou para você?"
    - Arquivo contém "inscricao-form" na função scrollToForm e no CTA
    - `npx tsc --noEmit` sem erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Alterações de copy e adição de CTA estático — sem inputs de usuário, sem chamadas de API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
- `grep "Acesso exclusivo · Bulinho esteve lá" src/components/ApartamentoGallery.tsx` retorna 1 match
- `grep -c "caption:" src/components/ApartamentoGallery.tsx` retorna 8
- `grep "Quer saber quais animais" src/components/ApartamentoGallery.tsx` retorna 1 match
- `npx tsc --noEmit` sem erros
</verification>

<success_criteria>
A galeria exibe heading com prova social do Bulinho, todas as 8 fotos têm captions descritivos visíveis no hover, e um CTA ancora ao formulário imediatamente após a galeria.
</success_criteria>

<output>
Criar `.planning/phases/02-envasamento-bula-bulinho/02-02-SUMMARY.md` ao concluir.
</output>
