---
phase: 02-envasamento-bula-bulinho
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - src/components/BulaConfianca.tsx
  - src/App.tsx
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - CONV-06
  - COPY-01
must_haves:
  truths:
    - "A seção BulaConfianca é renderizada entre ApartamentoGallery e Footer"
    - "O eyebrow exibe 'Método · Campo a campo'"
    - "O H2 exibe 'A diferença entre comprar com a Bula e comprar sozinho'"
    - "Três colunas existem: 'Apartamento pessoal', 'Análise antes do martelo', 'Zero custo, zero risco'"
    - "Um CTA centralizado 'Garantir minha assessoria →' ancora ao #inscricao-form"
    - "Em mobile as colunas empilham verticalmente (flex-col)"
    - "Em desktop as colunas ficam em linha (lg:grid-cols-3)"
  artifacts:
    - path: src/components/BulaConfianca.tsx
      provides: "Seção de 3 colunas com diferenciais da Bula e CTA"
      contains: "A diferença entre comprar com a Bula e comprar sozinho"
      exports:
        - BulaConfianca
    - path: src/App.tsx
      provides: "Composição da página com BulaConfianca inserida"
      contains: "BulaConfianca"
  key_links:
    - from: src/App.tsx
      to: src/components/BulaConfianca.tsx
      via: "import e JSX entre ApartamentoGallery e Footer"
      pattern: "<BulaConfianca"
    - from: src/components/BulaConfianca.tsx
      to: "#inscricao-form"
      via: "scrollIntoView onClick no CTA"
      pattern: "inscricao-form"
---

<objective>
Criar o componente `BulaConfianca.tsx` com três colunas de diferenciais e um CTA centralizado, depois integrá-lo em `App.tsx` entre `<ApartamentoGallery />` e `<Footer />`.

Purpose: Após ver as fotos do apartamento, o usuário precisa de uma resposta clara a "por que devo usar a Bula?". Esta seção entrega os três diferenciadores (presença física, análise prévia, custo zero) num formato scannable, com CTA imediato para não perder o momento.

Output: Novo arquivo `src/components/BulaConfianca.tsx` exportando `BulaConfianca`. `src/App.tsx` importa e posiciona o componente.
</objective>

<execution_context>
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/.planning/ROADMAP.md
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/App.tsx
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/ApartamentoGallery.tsx
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componente BulaConfianca.tsx</name>
  <files>src/components/BulaConfianca.tsx</files>
  <read_first>
    - src/components/Footer.tsx — linhas 1–31: padrão de scrollToForm, estrutura do eyebrow (classe `text-[11px] font-black uppercase tracking-[0.18em] text-white/50`), e botão CTA primário (classes `rounded-xl bg-white px-6 py-3.5 text-sm font-black`).
    - src/components/ApartamentoGallery.tsx — linhas 98–113: padrão do bloco de heading (`section.bg-neutral-950.py-14.sm:py-20 > div.mx-auto.max-w-7xl > div.mb-10.px-5.sm:px-8`).
  </read_first>
  <action>
    Criar `src/components/BulaConfianca.tsx` com o seguinte conteúdo:

    Import: `import { ArrowRight } from 'lucide-react'`

    Função auxiliar de scroll (mesmo padrão dos outros componentes):
    `function scrollToForm(e: React.MouseEvent) { e.preventDefault(); document.getElementById('inscricao-form')?.scrollIntoView({ behavior: 'smooth' }) }`

    Array de colunas (const interno ao módulo, antes do componente):
    ```
    const DIFERENCIAIS = [
      {
        numero: '01',
        titulo: 'Apartamento pessoal',
        descricao: 'Bulinho esteve fisicamente no curral. Avaliou casco, cobertura, conformação. Indicação baseada em contato real, não em PDF.',
      },
      {
        numero: '02',
        titulo: 'Análise antes do martelo',
        descricao: 'Nossa análise chega antes do leilão começar. Você já sabe quais touros priorizar, sem pressão de decidir em segundos.',
      },
      {
        numero: '03',
        titulo: 'Zero custo, zero risco',
        descricao: 'Assessoria 100% gratuita. Frete incluso até sua fazenda. 30x no boleto. Se não gostou do animal, não compra.',
      },
    ]
    ```

    Componente `BulaConfianca`:
    - Wrapper: `<section className="bg-neutral-950 py-14 sm:py-20 border-t border-white/8">`
    - Inner: `<div className="mx-auto max-w-7xl px-5 sm:px-8">`
    - Heading block (mesmos padrões do design system):
      - Eyebrow: `<span className="text-[11px] font-bold uppercase tracking-widest text-white/40">Método · Campo a campo</span>`
      - H2: `<h2 className="mt-2 text-3xl font-black tracking-tight text-white sm:text-4xl">A diferença entre comprar com a Bula e comprar sozinho</h2>`
    - Grid de colunas: `<div className="mt-10 grid gap-8 sm:grid-cols-2 lg:grid-cols-3">`
    - Cada coluna mapeia `DIFERENCIAIS` para:
      ```jsx
      <div key={d.numero} className="flex flex-col gap-3 rounded-xl border border-white/10 bg-white/5 p-6">
        <span className="text-[11px] font-black uppercase tracking-widest text-white/30">{d.numero}</span>
        <h3 className="text-lg font-black text-white leading-tight">{d.titulo}</h3>
        <p className="text-sm leading-relaxed text-white/55">{d.descricao}</p>
      </div>
      ```
    - CTA centralizado abaixo do grid: `<div className="mt-10 text-center">`
      ```jsx
      <a
        href="#inscricao"
        onClick={scrollToForm}
        className="inline-flex items-center justify-center gap-2 rounded-xl bg-white px-6 py-3.5 text-sm font-black shadow-sm transition-all hover:-translate-y-0.5 hover:bg-white/90"
        style={{ color: '#111111' }}
      >
        Garantir minha assessoria
        <ArrowRight className="h-4 w-4" />
      </a>
      ```

    Exportar: `export function BulaConfianca() { ... }`
  </action>
  <verify>
    <automated>grep -c "A diferença entre comprar com a Bula" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/BulaConfianca.tsx && grep -c "Método · Campo a campo" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/BulaConfianca.tsx && grep -c "Apartamento pessoal" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/BulaConfianca.tsx && grep -c "Zero custo, zero risco" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/BulaConfianca.tsx && grep -c "Garantir minha assessoria" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/BulaConfianca.tsx && grep -c "inscricao-form" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/BulaConfianca.tsx</automated>
  </verify>
  <done>
    - Arquivo `src/components/BulaConfianca.tsx` existe
    - Contém eyebrow "Método · Campo a campo"
    - Contém H2 "A diferença entre comprar com a Bula e comprar sozinho"
    - Contém as 3 colunas: "Apartamento pessoal", "Análise antes do martelo", "Zero custo, zero risco"
    - Contém CTA "Garantir minha assessoria" com scroll para "inscricao-form"
    - `npx tsc --noEmit` sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrar BulaConfianca no App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx — ler o arquivo completo (30 linhas). Identificar a posição de `<ApartamentoGallery />` (linha 23) e `<Footer />` (linha 25). O componente BulaConfianca deve ser inserido entre eles, dentro do `<main>`.
  </read_first>
  <action>
    1. Adicionar o import no topo do arquivo, após os imports existentes de componentes:
       `import { BulaConfianca } from './components/BulaConfianca'`

    2. No JSX de `App`, dentro de `<main>`, após `<ApartamentoGallery />` e antes do fechamento `</main>`, inserir `<BulaConfianca />`. A ordem final dentro de `<main>` deve ser:
       ```
       <div className="relative bg-black hero-bg" ...>
         <Form />
       </div>
       <LeilaoCard />
       <ApartamentoGallery />
       <BulaConfianca />
       ```
       O `<Footer />` fica fora de `<main>`, imediatamente após o fechamento `</main>`, sem alteração.

    Não alterar: estilos do wrapper, bgPhoto, ordem dos outros componentes, estrutura do `<footer>`.
  </action>
  <verify>
    <automated>grep -c "BulaConfianca" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/App.tsx && npx tsc --noEmit --project /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/tsconfig.json 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <done>
    - `grep "BulaConfianca" src/App.tsx` retorna 2 matches (import + JSX)
    - `<BulaConfianca />` aparece após `<ApartamentoGallery />` no JSX
    - `npx tsc --noEmit` sem erros de TypeScript
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Componente puramente presentacional — sem inputs, sem API calls, sem dados de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado neste plano; ArrowRight já está no lucide-react existente |
</threat_model>

<verification>
- `ls src/components/BulaConfianca.tsx` existe
- `grep "BulaConfianca" src/App.tsx` retorna 2 matches
- `grep "A diferença entre comprar com a Bula" src/components/BulaConfianca.tsx` retorna 1 match
- `npx tsc --noEmit` sem erros
- Build: `npm run build` sem erros
</verification>

<success_criteria>
O componente BulaConfianca existe, renderiza 3 colunas com diferenciais da Bula, um CTA de conversão, e é inserido na página entre a galeria e o footer.
</success_criteria>

<output>
Criar `.planning/phases/02-envasamento-bula-bulinho/02-05-SUMMARY.md` ao concluir.
</output>
