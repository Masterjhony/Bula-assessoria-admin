---
phase: 02-envasamento-bula-bulinho
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Form.tsx
autonomous: true
requirements:
  - CONV-04
  - COPY-01
must_haves:
  truths:
    - "Step 1 do formulário não contém 'Voce' (sem acento)"
    - "Step 1 do formulário não contém a palavra 'ofertas'"
    - "Step 1 do formulário contém 'indicações de animais direto no WhatsApp'"
    - "A coluna esquerda (hero content) exibe um blockquote com a frase do Bulinho"
  artifacts:
    - path: src/components/Form.tsx
      provides: "Form com copy corrigido no Step 1 e quote do Bulinho na coluna esquerda"
      contains: "Eu apartei cada touro desse leilão"
  key_links:
    - from: "coluna esquerda (div.lg:w-[48%])"
      to: "blockquote Bulinho"
      via: "JSX após ul de bullets"
      pattern: "Eu apartei cada touro"
---

<objective>
Corrigir dois erros de copy no Step 1 do formulário ("Voce" → "Você", "ofertas" → "indicações de animais direto no WhatsApp") e adicionar um blockquote de prova social do Bulinho na coluna esquerda do formulário, visível em desktop.

Purpose: Os erros de copy no formulário prejudicam credibilidade no momento de máxima intenção. O quote do Bulinho na coluna esquerda cria o último empurrão de confiança antes do usuário preencher os dados.

Output: `src/components/Form.tsx` com Step 1 corrigido e bloco blockquote inserido na coluna esquerda após a `<ul>` de bullets.
</objective>

<execution_context>
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/.planning/ROADMAP.md
@/Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir copy do Step 1 e adicionar quote do Bulinho na coluna esquerda</name>
  <files>src/components/Form.tsx</files>
  <read_first>
    - src/components/Form.tsx — ler as linhas 376–422 (Step 1 JSX) para localizar `h3` e `p` com os erros. Depois ler linhas 252–337 (coluna esquerda) para localizar o fechamento da `</ul>` de bullets (linha ~313) e a div `border-t` de stats que vem em seguida.
  </read_first>
  <action>
    1. CORREÇÃO Step 1 — dentro do bloco `{step === 1 && ( <> ... </> )}`:

    a) Linha com `<h3>`: trocar `"Responda e receba ofertas!"` por `"Responda e receba indicações!"`. (Elimina "ofertas" do h3 também.)

    b) Linha com `<p className="text-white/35 text-sm mb-5">`: trocar o conteúdo `"Voce recebe as ofertas pelo celular! Cadastre-se"` por `"Você recebe indicações de animais direto no WhatsApp!"`.

    2. QUOTE do Bulinho — na coluna esquerda `div.lg:w-[48%]`, localizar o fechamento da `</ul>` dos bullets (a lista que termina com "Condição especial para renovar a bateria de touros"). Após esse `</ul>` e antes da div `border-t` que contém os stats (div com `border-t border-white/12 pt-6`), inserir:

    ```jsx
    {/* Quote do Bulinho */}
    <blockquote className="mt-8 border-l-2 border-white/20 pl-4">
      <p className="text-white/80 text-sm leading-relaxed italic">
        "Eu apartei cada touro desse leilão. Sei qual vale, qual não vale."
      </p>
      <footer className="mt-2 text-white/40 text-xs font-semibold uppercase tracking-wider">
        — Bulinho, assessor da Bula
      </footer>
    </blockquote>
    ```

    Não alterar: imports, lógica de validação, handleChange, handleUFChange, goTo, onSubmit, Steps 2 e 3, coluna direita (formulário), trust signal, event quick info.
  </action>
  <verify>
    <automated>grep -c "Você recebe indicações de animais direto no WhatsApp" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Form.tsx && grep -c "Eu apartei cada touro desse leilão" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Form.tsx && grep -v "indicações" /Users/joaogabrielsantosdosanjos/Documents/FORMULA-DO-BOI/pagina_forms-JMP/src/components/Form.tsx | grep -c "ofertas" || true</automated>
  </verify>
  <done>
    - Step 1 `<p>` contém "Você recebe indicações de animais direto no WhatsApp!"
    - "Voce " (sem acento + espaço) não aparece no arquivo
    - "ofertas" não aparece em contexto de copy do formulário
    - Blockquote contém "Eu apartei cada touro desse leilão. Sei qual vale, qual não vale."
    - Footer do blockquote contém "— Bulinho, assessor da Bula"
    - `npx tsc --noEmit` sem erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuário → Form | Inputs de nome/email/whatsapp/uf/cidade/momento/cabecas/interesse — validados pelo `validateStep` existente, sem alteração neste plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | Form submit | accept | `submitForm` é console.log + timeout — sem envio real de dados neste estágio |
| T-02-04-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
- `grep "Você recebe indicações de animais direto no WhatsApp" src/components/Form.tsx` retorna 1 match
- `grep "Eu apartei cada touro desse leilão" src/components/Form.tsx` retorna 1 match
- `grep "Voce " src/components/Form.tsx` retorna 0 matches
- `npx tsc --noEmit` sem erros
</verification>

<success_criteria>
Step 1 do formulário exibe copy correto sem erros de acentuação ou palavra errada. A coluna esquerda exibe o blockquote do Bulinho entre os bullets e os stats.
</success_criteria>

<output>
Criar `.planning/phases/02-envasamento-bula-bulinho/02-04-SUMMARY.md` ao concluir.
</output>
