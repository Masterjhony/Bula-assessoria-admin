---
phase: 03-mega-evento-eao-baviera
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/content.ts
  - src/components/Form.tsx
  - public/obrigado-jmp.html
autonomous: true
requirements:
  - EAO-08
  - EAO-09
  - EAO-10
must_haves:
  truths:
    - "O badge do hero exibe 'ASSESSORIA GRATUITA · 09 A 12 DE JULHO'"
    - "O headline e o benefitsTitle não afirmam mais que a Bula selecionou/apartou os animais deste leilão especificamente — vendem a assessoria de compra gratuita"
    - "Os 5 itens de benefits estão reposicionados em torno do serviço de assessoria (equipe, leitura de genética, escolha de animais, habilitação e pós-leilão), sem menção a 'seleção avaliada'/'apartada pela Bula' nem a condições do evento (desconto/frete)"
    - "stats do hero exibe 2 itens: GRÁTIS/ASSESSORIA DE COMPRA e 09–12 JUL/MEGA BAVIERA"
    - "Step 1 do formulário exibe 'Faça seu cadastro!' e o subtexto sobre contato via WhatsApp, sem a palavra 'ofertas'"
    - "O título do form exibe 'Receba a assessoria' / 'gratuita da Bula'"
    - "O botão de submit exibe 'QUERO MINHA ASSESSORIA →', com o estado de loading 'Enviando…' inalterado"
    - "public/obrigado-jmp.html tem <title>Cadastro Realizado — 13º Mega Baviera</title>"
    - "Step 2, o corpo do Step 3 (Momento/Cabeças/Interesse/Quantidade/Inscrição Estadual/checkbox de consentimento WhatsApp), validateStep, submitForm, onSubmit, handlers, o bloco de identidade Bula+EAO e o bloco Event quick info permanecem intocados"
  artifacts:
    - path: "src/content.ts"
      provides: "DEFAULT_CONTENT.hero reposicionado de 'seleção apartada pela Bula' (falso) para 'assessoria de compra gratuita' (correto)"
      contains: "Assessoria de compra 100% gratuita"
    - path: "src/components/Form.tsx"
      provides: "Step 1, título do form e botão de submit atualizados para a nova posição de assessoria"
      contains: "QUERO MINHA ASSESSORIA"
    - path: "public/obrigado-jmp.html"
      provides: "<title> atualizado para o evento 13º Mega Baviera (leftover pré-fase-3 do leilão JMP removido)"
      contains: "13º Mega Baviera"
  key_links:
    - from: "src/content.ts (DEFAULT_CONTENT.hero)"
      to: "src/components/Form.tsx (Form({ hero }))"
      via: "wiring existente via App.tsx — hero.badge/headline/valueProp/benefitsTitle/benefits/stats renderizados sem mudança de código"
      pattern: "hero\\.(badge|headline|benefits|stats)"
    - from: "src/components/Form.tsx (onSubmit → OBRIGADO_PAGE_URL)"
      to: "public/obrigado-jmp.html"
      via: "window.location.href = OBRIGADO_PAGE_URL após submit bem-sucedido — não alterado por este plano"
      pattern: "OBRIGADO_PAGE_URL"
---

<objective>
Corrigir uma afirmação comercial falsa no hero da landing page: o copy atual (`benefitsTitle: 'Genética EAO\nApartada pela Bula'` e o benefit `'Seleção avaliada animal por animal'`) dá a entender que a Bula Assessoria selecionou/apartou pessoalmente os animais do 13º Mega Evento EAO Baviera — o cliente confirmou que isso é FALSO. Este plano reposiciona toda a coluna hero e trechos de copy do formulário de "Bula selecionou os lotes" para "Bula oferece assessoria de compra gratuita" (a Bula ajuda o comprador a entender a genética e decidir, sem reivindicar crédito pela seleção dos lotes do leilão), e corrige um `<title>` remanescente do leilão Nelore JMP na página de agradecimento.

Purpose: Mesma régua de precisão já aplicada nesta fase para não inventar contagem de lotes ou condições comerciais (desconto/frete) que não foram confirmadas — aqui a régua se aplica a uma alegação de serviço: a página não pode afirmar que a Bula fez algo que ela não fez.

Output: `src/content.ts` com `DEFAULT_CONTENT.hero` reposicionado (badge, headline, valueProp, valuePropStrong, benefitsTitle, benefits, stats — `locationLine1`/`locationLine2` ficam como estão); `src/components/Form.tsx` com Step 1 (header + subtexto), título do form e texto do botão de submit atualizados; `public/obrigado-jmp.html` com `<title>` corrigido. `src/components/Footer.tsx` é apenas verificado (sem alegação falsa encontrada), não editado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-mega-evento-eao-baviera/03-01-SUMMARY.md
@.planning/phases/03-mega-evento-eao-baviera/03-02-SUMMARY.md
@src/content.ts
@src/components/Form.tsx
@public/obrigado-jmp.html
</context>

<fora-de-escopo>
## Fora de escopo (não tocar neste plano)

- **Step 2** (UF/Cidade), **corpo do Step 3** (Momento na Pecuária/Cabeças/Inscrição Estadual/Interesse/Quantidade/checkbox de consentimento WhatsApp), `validateStep`, `submitForm`, `onSubmit`, `handleChange`, `handleUFChange`, `handleWhatsappConsentChange`, `goTo`, `UFCombobox`, captura de UTM, chamadas de analytics — nenhuma lógica de formulário é alterada, só as strings de copy listadas nas Tasks abaixo.
- **Bloco de identidade Bula + EAO** (logo real `/logo-eao-white.png`, adicionado em um passo untracked anterior) — já correto, não tocar.
- **Bloco "Event quick info"** (data/local no rodapé do form) — já correto ("09 a 12 Jul · Fazenda Baviera" / "Itagibá / BA"), não tocar.
- **`locationLine1`/`locationLine2`** do hero (`'Itagibá / BA'` / `'Fazenda Baviera'`) — cliente confirmou que ficam como estão.
- **`src/constants.ts`** (`WHATSAPP_GROUP_URL`, `OBRIGADO_PAGE_URL`) — mesmo follow-up conhecido já sinalizado nas Plans 03-01/03-02, não corrigido aqui.
- **`src/components/Footer.tsx`** — verificado durante o planejamento: a única menção a "apartações" é uma descrição genérica da empresa ("Assessoria especializada em estratégias comerciais, apartações e compra de touros e matrizes nos principais leilões do Brasil"), não uma alegação específica sobre os lotes do 13º Mega Baviera. Não editar; a Task 3 só re-confirma isso no momento da execução (ver Task 3).
- **Nova seção "Sobre o leilão"** (bloco de contexto explicando que a Bula não apartou este evento especificamente) — o cliente floatou essa ideia como possível adição futura ("Parte 3" do brief), mas não faz parte do mapeamento campo-a-campo de copy ("Parte 2") que este plano implementa. Não construir nova estrutura/seção aqui — é uma boa ideia para uma fase futura, não para este plano.
- **Checkbox de consentimento WhatsApp, imagem do flyer, estrutura/links sociais do Footer, fix de background mobile do hero, troca do logo EAO por asset real** — todos já feitos em plans anteriores (03-01/03-02) ou em edições untracked fora deste fluxo de planejamento; não retocar.
- **Discount/frete** (`'Até 10% de desconto à vista'`, `'Frete grátis para animais PO'`) — são condições do evento, não do serviço de assessoria da Bula; o novo brief do cliente as remove da lista de `benefits` da Bula deliberadamente (ver Task 1). Não recolocar essas duas linhas em `benefits` neste plano; se precisarem aparecer em algum lugar da página, isso é decisão de uma fase futura (ex.: Footer ou um bloco "sobre o leilão"), não deste plano.
</fora-de-escopo>

<tasks>

<task type="auto">
  <name>Task 1: Reposicionar DEFAULT_CONTENT.hero de "seleção apartada pela Bula" para "assessoria de compra gratuita"</name>
  <files>src/content.ts</files>
  <read_first>
    - src/content.ts — localizar `DEFAULT_CONTENT.hero`, especificamente os campos `badge`, `headline`, `valueProp`, `valuePropStrong`, `benefitsTitle`, o array `benefits` (hoje 5 itens, incluindo os dois últimos sobre desconto/frete), e o array `stats` (hoje 1 item, só data). `locationLine1`/`locationLine2` não mudam.
  </read_first>
  <action>
    Substituir os seguintes campos de `DEFAULT_CONTENT.hero`, verbatim — copy já finalizada pelo cliente, sem redação adicional necessária:

    - `badge`: `'ASSESSORIA GRATUITA · 09 A 12 DE JULHO'`
    - `headline`: `'Compre bem no\nMega Baviera com\na Bula ao seu lado.'` (mantém a convenção `\n` já usada; renderiza via `MultiLine` em `Form.tsx`, sem mudança de código)
    - `valueProp`: `'A equipe de assessores da Bula te ajuda a entender a genética, escolher os animais certos e dar o lance certo no 13º Mega Baviera.'`
    - `valuePropStrong`: `'Assessoria gratuita. Sem compromisso.'`
    - `benefitsTitle`: `'Uma Equipe de\nAssessores do Seu\nLado no Mega Baviera'`
    - `benefits`: substituir o array INTEIRO (os 5 itens atuais, incluindo os dois últimos sobre desconto à vista e frete grátis) pelos 5 itens abaixo, nesta ordem exata:
      1. `{ text: 'Assessoria de compra 100% gratuita', strong: true }`
      2. `{ text: 'Equipe de assessores com você no pregão' }`
      3. `{ text: 'Leitura da genética e dos números do catálogo' }`
      4. `{ text: 'Ajuda pra escolher os animais certos pro seu rebanho' }`
      5. `{ text: 'Apoio na habilitação e no pós-leilão', strong: true }`
    - `stats`: substituir o array atual (1 item, só a data) por DOIS itens: `[{ value: 'GRÁTIS', label: 'ASSESSORIA DE COMPRA' }, { value: '09–12 JUL', label: 'MEGA BAVIERA' }]` — mesmo travessão curto "–" (en-dash) já usado hoje entre "09" e "12".

    Motivo da mudança (correção de compliance, não estilística): o copy atual afirma que a Bula selecionou/apartou os lotes deste leilão especificamente (`benefitsTitle: 'Genética EAO\nApartada pela Bula'`, benefit `'Seleção avaliada animal por animal'`) — confirmado como FALSO pelo cliente. O novo copy vende o SERVIÇO DE ASSESSORIA DE COMPRA gratuita (a Bula ajuda a entender/decidir, não reivindica ter selecionado os animais).

    NÃO alterar: `locationLine1` (`'Itagibá / BA'`), `locationLine2` (`'Fazenda Baviera'`), `backgroundUrl`, `whatsappGroupUrl`, o array `blocks`, nem nenhuma interface (`JmpHero`, `JmpBenefit`, `JmpStat` etc.). Não recolocar as linhas de desconto/frete removidas de `benefits` — são condições do evento, fora do escopo deste plano (ver `<fora-de-escopo>`).
  </action>
  <verify>
    <automated>grep -F -c "badge: 'ASSESSORIA GRATUITA · 09 A 12 DE JULHO'" src/content.ts && grep -F -c "headline: 'Compre bem no\nMega Baviera com\na Bula ao seu lado.'" src/content.ts && grep -F -c "valuePropStrong: 'Assessoria gratuita. Sem compromisso.'" src/content.ts && grep -F -c "Assessoria de compra 100% gratuita" src/content.ts && grep -F -c "Apoio na habilitação e no pós-leilão" src/content.ts && grep -F -c "value: 'GRÁTIS', label: 'ASSESSORIA DE COMPRA'" src/content.ts && grep -F -c "locationLine1: 'Itagibá / BA'" src/content.ts && test $(grep -c "Apartada pela Bula" src/content.ts) -eq 0 && test $(grep -c "10% de desconto" src/content.ts) -eq 0 && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>
    - `badge`, `headline`, `valueProp`, `valuePropStrong`, `benefitsTitle` refletem a nova posição de assessoria de compra, sem alegação de seleção/aparte
    - `benefits` tem exatamente os 5 itens novos, na ordem especificada — nenhum item de desconto/frete
    - `stats` tem exatamente 2 itens (GRÁTIS/ASSESSORIA DE COMPRA e 09–12 JUL/MEGA BAVIERA)
    - `locationLine1`/`locationLine2` permanecem `'Itagibá / BA'` / `'Fazenda Baviera'`
    - `npx tsc --noEmit -p tsconfig.app.json` sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar Step 1, título do form e botão de submit em Form.tsx para a nova posição de assessoria</name>
  <files>src/components/Form.tsx</files>
  <read_first>
    - src/components/Form.tsx — dentro do JSX de `{step === 1 && (...)}`, o `<h3>` (hoje "Responda e receba ofertas!") e o `<p className="text-white/35 text-sm mb-5">` logo abaixo (hoje "Voce recebe as ofertas pelo celular! Cadastre-se"); dentro do bloco "Form header" (`<div className="mb-7">`), o `<h2>` com duas linhas via `<br />` (hoje "Garanta sua vaga" / `<span className="text-white/60 font-bold">no Mega Evento EAO</span>`); dentro do JSX de `{step === 3 && (...)}`, o texto do botão de submit dentro do `<span className="relative z-10 ...">` condicional (hoje `'QUERO PARTICIPAR! →'` no ramo não-loading — o ramo `loading` com `<Loader2 />Enviando…` NÃO muda).
  </read_first>
  <action>
    Quatro edições, todas dentro de `src/components/Form.tsx`, nenhuma outra:

    1. STEP 1 — HEADER: trocar o `<h3>` de `Responda e receba ofertas!` para `Faça seu cadastro!`.
    2. STEP 1 — SUBTEXTO: trocar o `<p>` logo abaixo de `Voce recebe as ofertas pelo celular! Cadastre-se` para `É rápido. A equipe Bula recebe seus dados e fala com você direto no WhatsApp.`.
    3. TÍTULO DO FORM: trocar as duas linhas do `<h2>` — primeira linha (antes do `<br />`) de `Garanta sua vaga` para `Receba a assessoria`; segunda linha (dentro do `<span className="text-white/60 font-bold">`, mesma classe mantida) de `no Mega Evento EAO` para `gratuita da Bula`.
    4. BOTÃO DE SUBMIT: no ramo não-loading do `{loading ? (...) : (...)}` dentro do botão de submit, trocar `QUERO PARTICIPAR! →` para `QUERO MINHA ASSESSORIA →`. NÃO alterar o ramo `loading` (`<Loader2 className="w-5 h-5 animate-spin" />Enviando…`) nem a estrutura condicional.

    NÃO alterar, sob nenhuma hipótese: o eyebrow `Grátis · Sem compromisso` acima do título (já renderiza em maiúsculas visualmente via a classe Tailwind `uppercase` — o texto-fonte não precisa mudar, nenhuma edição aqui), Step 2 inteiro, o corpo do Step 3 (Momento na Pecuária/Cabeças/Inscrição Estadual/Interesse/Quantidade/checkbox de consentimento WhatsApp — incluindo o texto "Autorizo a Bula Assessoria..."), `validateStep`, `submitForm`, `onSubmit`, `handleChange`, `handleUFChange`, `handleWhatsappConsentChange`, `goTo`, `UFCombobox`, captura de UTM, analytics, o bloco de identidade Bula+EAO (logo real, não tocar), e o bloco "Event quick info" (data/local, já corretos).
  </action>
  <verify>
    <automated>grep -F -c "Faça seu cadastro!" src/components/Form.tsx && grep -F -c "É rápido. A equipe Bula recebe seus dados e fala com você direto no WhatsApp." src/components/Form.tsx && grep -F -c "Receba a assessoria" src/components/Form.tsx && grep -F -c "gratuita da Bula" src/components/Form.tsx && grep -F -c "QUERO MINHA ASSESSORIA" src/components/Form.tsx && grep -F -c "Enviando…" src/components/Form.tsx && test $(grep -c "Responda e receba ofertas" src/components/Form.tsx) -eq 0 && test $(grep -c "QUERO PARTICIPAR" src/components/Form.tsx) -eq 0 && grep -F -c "Autorizo a Bula Assessoria a entrar em contato comigo no WhatsApp" src/components/Form.tsx && grep -F -c "/logo-eao-white.png" src/components/Form.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>
    - Step 1 exibe `Faça seu cadastro!` e o novo subtexto sobre contato via WhatsApp — "ofertas" não aparece mais nesse contexto
    - Título do form exibe `Receba a assessoria` / `gratuita da Bula`
    - Botão de submit exibe `QUERO MINHA ASSESSORIA →`; estado de loading `Enviando…` inalterado
    - Checkbox de consentimento WhatsApp, identidade Bula+EAO e Event quick info seguem presentes e intactos
    - `npx tsc --noEmit -p tsconfig.app.json` sem erros
  </done>
</task>

<task type="auto">
  <name>Task 3: Corrigir título de obrigado-jmp.html, reconfirmar Footer.tsx e verificação final</name>
  <files>public/obrigado-jmp.html</files>
  <read_first>
    - public/obrigado-jmp.html — linha do `<title>` (hoje `Cadastro Realizado — 10º Leilão Nelore JMP`). Reler rapidamente `<h1>`, `.subtitle`, e os 3 `.step` (títulos e descrições) para confirmar que nenhum menciona JMP ou uma alegação de aparte específica deste leilão — já confirmado durante o planejamento, mas reconfirme antes de decidir não tocar.
    - src/components/Footer.tsx (123 linhas) — já verificado durante o planejamento: a única menção a "apartações" é a descrição genérica da empresa ("Assessoria especializada em estratégias comerciais, apartações e compra de touros e matrizes nos principais leilões do Brasil"), não uma alegação específica sobre os lotes do 13º Mega Baviera. Releia para reconfirmar antes de decidir não editar.
  </read_first>
  <action>
    1. TÍTULO — em `public/obrigado-jmp.html`, trocar `<title>Cadastro Realizado — 10º Leilão Nelore JMP</title>` para `<title>Cadastro Realizado — 13º Mega Baviera</title>`. Não alterar nenhum outro texto do arquivo (h1, subtitle, os 3 steps, botão de WhatsApp, script de countdown/analytics/PostHog) — nenhum deles menciona JMP ou faz alegação de aparte específica deste leilão.

    2. FOOTER — NÃO editar `src/components/Footer.tsx` neste plano. A releitura no `read_first` serve só para reconfirmar que não há alegação de que a Bula selecionou/apartou os lotes do 13º Mega Baviera especificamente. Se, ao reler o arquivo no momento da execução, uma alegação desse tipo for encontrada (ex.: texto novo mencionando este leilão como "apartado pela Bula"), reporte no SUMMARY em vez de editar silenciosamente — este plano não inclui uma correção pré-escrita para o Footer.

    3. VERIFICAÇÃO FINAL — rodar o build completo de produção e uma bateria de greps de regressão confirmando que Step 2, o corpo do Step 3, `validateStep`, `submitForm`, o bloco de identidade e o "Event quick info" permanecem intocados, e que o novo copy das Tasks 1-2 está presente.
  </action>
  <verify>
    <automated>grep -F -c "<title>Cadastro Realizado — 13º Mega Baviera</title>" public/obrigado-jmp.html && test $(grep -c "Nelore JMP" public/obrigado-jmp.html) -eq 0 && grep -F -c "Momento na Pecuária" src/components/Form.tsx && grep -F -c "Sua localização" src/components/Form.tsx && grep -F -c "Assessoria de compra 100% gratuita" src/content.ts && grep -F -c "QUERO MINHA ASSESSORIA" src/components/Form.tsx && npx tsc --noEmit -p tsconfig.app.json && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `public/obrigado-jmp.html` tem `<title>Cadastro Realizado — 13º Mega Baviera</title>`; nenhuma menção a "Nelore JMP" permanece no arquivo
    - `src/components/Footer.tsx` reconfirmado sem alegação falsa de aparte — não editado (ou, se uma alegação foi encontrada, reportada no SUMMARY)
    - Step 2 ("Sua localização") e o corpo do Step 3 ("Momento na Pecuária") permanecem com seus textos originais — sem regressão
    - `npx tsc --noEmit -p tsconfig.app.json` sem erros
    - `npm run build` conclui com exit 0 (tsc -b + vite build), bundle gerado em `dist/`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Mudança puramente de conteúdo estático (strings de copy em `content.ts`/`Form.tsx`/`obrigado-jmp.html`). Sem inputs de usuário novos, sem chamadas de API novas, sem dados sensíveis. Nenhuma lógica de submit/validação é tocada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Repudiation | Alegações comerciais no hero (`DEFAULT_CONTENT.hero`) | mitigate | Este plano existe justamente para remover uma alegação de serviço falsa ("Bula apartou os lotes deste leilão") e substituí-la pela alegação correta (assessoria de compra gratuita), confirmada pelo cliente |
| T-03-03-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
- `grep "ASSESSORIA GRATUITA · 09 A 12 DE JULHO" src/content.ts` retorna 1 match
- `grep "Assessoria de compra 100% gratuita" src/content.ts` retorna 1 match
- `grep "Apartada pela Bula" src/content.ts` retorna 0 matches
- `grep "Faça seu cadastro!" src/components/Form.tsx` retorna 1 match
- `grep "QUERO MINHA ASSESSORIA" src/components/Form.tsx` retorna 1 match
- `grep "QUERO PARTICIPAR" src/components/Form.tsx` retorna 0 matches
- `grep "<title>Cadastro Realizado — 13º Mega Baviera</title>" public/obrigado-jmp.html` retorna 1 match
- `grep "Nelore JMP" public/obrigado-jmp.html` retorna 0 matches
- `npx tsc --noEmit -p tsconfig.app.json` sem erros
- `npm run build` conclui com exit 0
</verification>

<success_criteria>
A landing page não afirma mais que a Bula selecionou/apartou os animais do 13º Mega Evento EAO Baviera — o hero e o formulário vendem a assessoria de compra gratuita (equipe de assessores, leitura de genética, escolha de animais, apoio na habilitação e pós-leilão), copy fornecido verbatim pelo cliente. A página de agradecimento não tem mais nenhum resquício textual do leilão Nelore JMP. Nenhuma lógica de validação/submit do formulário foi alterada; `npx tsc --noEmit -p tsconfig.app.json` e `npm run build` passam limpos.
</success_criteria>

<output>
Criar `.planning/phases/03-mega-evento-eao-baviera/03-03-SUMMARY.md` ao concluir.
</output>
