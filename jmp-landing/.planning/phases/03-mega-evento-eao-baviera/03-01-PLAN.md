---
phase: 03-mega-evento-eao-baviera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.ts
  - src/components/Form.tsx
  - index.html
autonomous: true
requirements:
  - EAO-01
  - EAO-02
  - EAO-03
  - EAO-04
  - EAO-05
  - EAO-06
must_haves:
  truths:
    - "O background do hero exibe a arte do 13º Mega Evento EAO Baviera (foto-leilao-eao.jpeg), não mais a foto do Bulinho"
    - "O badge de urgência exibe 'VAGAS ABERTAS · 09 A 12 DE JULHO'"
    - "O headline principal e o benefitsTitle mencionam o Mega Evento EAO / Genética EAO, sem resquício de copy do Nelore JMP"
    - "Os 5 benefícios listados refletem a seleção e habilitação EAO (nenhum contém 'JMP' ou '1.000 touros')"
    - "O único stat exibido é a data do evento ('09–12 JUL' / '2026 · 4 dias'), sem contagem numérica de lotes inventada"
    - "O bloco de localização do hero exibe 'Itagibá / BA' e 'Fazenda Baviera'"
    - "A identidade ao lado do logo Bula exibe o wordmark de texto 'EAO BAVIERA'; o logo JMP e seu import não utilizado foram removidos"
    - "Nenhum resquício textual do logo antigo (`jmpLogo`, `alt=\"JMP\"`, `no JMP 2026`) permanece em Form.tsx — mas `/api/jmp/lead`, `JmpHero` e `jmp_utm` continuam intactos (infraestrutura fora de escopo, não relacionada à marca)"
    - "O título do formulário lê 'Garanta sua vaga' / 'no Mega Evento EAO'"
    - "O bloco de info rápida do evento exibe '09 a 12 Jul · Fazenda Baviera' e 'Itagibá / BA'"
    - "O <title> da aba e o preload LCP do index.html referenciam o evento EAO Baviera e o asset foto-leilao-eao.jpeg"
    - "Nenhuma linha dos Steps 1–3, validateStep, submitForm, onSubmit, handleChange, handleUFChange ou goTo em Form.tsx foi alterada"
  artifacts:
    - path: "src/content.ts"
      provides: "DEFAULT_CONTENT.hero atualizado para o Mega Evento EAO Baviera"
      contains: "foto-leilao-eao.jpeg"
    - path: "src/components/Form.tsx"
      provides: "Identidade EAO Baviera, título do form e info rápida do evento atualizados; import jmpLogo removido"
      contains: "EAO BAVIERA"
    - path: "index.html"
      provides: "Preload LCP e <title> atualizados para o evento EAO Baviera"
      contains: "13º Mega Evento EAO Baviera — Inscrições"
  key_links:
    - from: "src/content.ts (hero.backgroundUrl)"
      to: "index.html (preload LCP href)"
      via: "ambos devem apontar para o mesmo asset estático, senão o navegador pré-carrega uma imagem que não é a de fundo real do hero"
      pattern: "foto-leilao-eao.jpeg"
    - from: "src/content.ts (DEFAULT_CONTENT.hero)"
      to: "src/components/Form.tsx (Form({ hero }: { hero: JmpHero }))"
      via: "App.tsx passa <Form hero={content.hero} /> — wiring existente, não alterado neste plano"
      pattern: "hero\\.(badge|headline|benefits|stats|locationLine)"
---

<objective>
Reskin de conteúdo e visual (sem tocar em campos, validação ou lógica de submit) da landing page, trocando toda a identidade e copy do leilão "Nelore JMP" pelo 13º Mega Evento EAO Baviera (genética Nelore PO, 09 a 12 de Julho de 2026, Fazenda Baviera, Itagibá/BA).

Purpose: A página hoje anuncia o leilão JMP de 13-14 de Junho de 2026 (evento já superado/irrelevante para o novo ciclo). O negócio precisa republicar a mesma página, com o mesmo formulário de captura de leads já validado em produção, mas com a arte e o copy do novo evento EAO Baviera — sem qualquer risco de regressão no formulário (campos, steps, validação, `submitForm` para `/api/jmp/lead`).

Output: `src/content.ts` com `DEFAULT_CONTENT.hero` inteiramente reescrito para o evento EAO Baviera; `src/components/Form.tsx` com o wordmark "EAO BAVIERA" substituindo o logo JMP, título do formulário e bloco de info rápida do evento atualizados; `index.html` com preload LCP e `<title>` atualizados. Nenhuma mudança em `src/constants.ts`, na lógica do `Form.tsx` (steps, `validateStep`, `submitForm`, `onSubmit`), ou em qualquer componente fora deste escopo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/content.ts
@src/components/Form.tsx
@index.html
</context>

<fora-de-escopo>
## Fora de escopo (não tocar neste plano)

- **`src/components/Form.tsx` — lógica e campos**: os bodies dos Steps 1 (Nome/E-mail/WhatsApp), 2 (UF/Cidade), 3 (Momento na Pecuária/Cabeças/Inscrição Estadual/Interesse/Quantidade), `validateStep`, `submitForm` (POST para `/api/jmp/lead`), `onSubmit`, `handleChange`, `handleUFChange`, `goTo`, `UFCombobox`, captura de UTM e chamadas de analytics (`trackForm*`) permanecem 100% intactos. Decisão travada do usuário — sem exceções.
- **`src/constants.ts`**: `WHATSAPP_GROUP_URL` e `OBRIGADO_PAGE_URL` continuam apontando para o grupo/página do JMP. Sinalizado como follow-up conhecido para uma fase futura — não corrigido aqui.
- **`src/content.ts` — `DEFAULT_CONTENT.whatsappGroupUrl` e `DEFAULT_CONTENT.blocks`** (array com os blocos `aparte-femeas`/`aparte-touros` do leilão JMP): nenhum componente atualmente montado por `App.tsx` consome esses campos (`App.tsx` só lê `content.hero.backgroundUrl` e passa `hero={content.hero}` para `Form`) — são dead code hoje, fora de escopo desta fase.
- **`src/components/Footer.tsx`, `LeilaoSections.tsx`, `PhotoGallery.tsx`, `ParticipeCTA.tsx`, `LeilaoCard.tsx`**: não importados por `App.tsx`, dead code, não tocar.
- **Nova seção de selos de parceiros** (Nelore, PMGZ, ANCP, Embrapa, Canal Rural etc.): não existe na página live hoje e não foi solicitada — ideia futura apenas, sem task planejada.
- **Reestruturação do formulário em fluxo de "habilitação + upload de documento"**: sugestão externa explicitamente rejeitada pelo usuário. O formulário de captura de leads atual permanece exatamente como está.
- Qualquer trabalho da Fase 2 (Envasamento Bula + História do Bulinho) — fora do escopo desta fase, não iniciado.
</fora-de-escopo>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever DEFAULT_CONTENT.hero em src/content.ts para o Mega Evento EAO Baviera</name>
  <files>src/content.ts</files>
  <read_first>
    - src/content.ts — arquivo completo (159 linhas). O objeto a substituir é `DEFAULT_CONTENT.hero`, hoje nas linhas ~60–80 (de `backgroundUrl: '/foto-bulinha-bg.jpeg'` até `locationLine2: 'Terra Nova Eventos'`). A interface `JmpHero` (linhas 22–33) define o shape exato — não alterar a interface.
  </read_first>
  <action>
    Substituir todos os campos do objeto `hero` dentro de `DEFAULT_CONTENT` pelos valores abaixo, mantendo exatamente o mesmo shape da interface `JmpHero` (mesma ordem de chaves não é obrigatória, mas todas as chaves existentes devem estar presentes):

    - `backgroundUrl`: `'/foto-leilao-eao.jpeg'` (asset já existe em `public/foto-leilao-eao.jpeg`).
    - `badge`: `'VAGAS ABERTAS · 09 A 12 DE JULHO'`.
    - `headline`: `'Habilite-se para o\nMega Evento EAO\ncom a Bula.'` — manter os `\n` como sequência de escape literal na string (duas letras, backslash+n), exatamente como o campo já fazia antes (`'Compre do leilão\napartado\npela Bula.'`); o componente `MultiLine` em `Form.tsx` já quebra essas linhas em `<br />`, não requer nenhuma mudança de código.
    - `valueProp`: `'A Bula avalia a seleção EAO, aparta os melhores animais para o seu rebanho e cuida de toda a sua habilitação no leilão.'`.
    - `valuePropStrong`: `'Grátis. Sem compromisso.'` (idêntico ao valor atual — manter sem alteração).
    - `benefitsTitle`: `'A Mega Seleção da\nGenética EAO,\nApartada pela Bula'` (mesma convenção de `\n` literal).
    - `benefits`: substituir o array inteiro de 5 itens pelos 5 objetos `{ text, strong? }` abaixo, nesta ordem exata:
      1. `{ text: 'Seleção EAO avaliada animal por animal' }`
      2. `{ text: 'Habilitação completa para dar lances', strong: true }`
      3. `{ text: 'Atendimento direto da equipe Bula Assessoria' }`
      4. `{ text: 'Análise de genética e valor justo antes do martelo cair' }`
      5. `{ text: 'Condição especial para renovar seu rebanho', strong: true }`
    - `stats`: substituir o array atual de 2 itens (`1.000 Touros PO` / `240 Bezerras PO`) por um único item: `[{ value: '09–12 JUL', label: '2026 · 4 dias' }]`. Usar o travessão curto "–" (en-dash, o mesmo caractere usado aqui) entre "09" e "12", não um hífen simples — não inventar nenhuma contagem numérica de lotes (o número real de lotes é desconhecido).
    - `locationLine1`: `'Itagibá / BA'`.
    - `locationLine2`: `'Fazenda Baviera'`.

    Não alterar: a interface `JmpHero` e as demais interfaces (`JmpFoto`, `JmpBenefit`, `JmpStat`, `JmpBlock`, `JmpContent`); o campo `whatsappGroupUrl` de `DEFAULT_CONTENT`; o array `blocks` de `DEFAULT_CONTENT` (blocos `aparte-femeas`/`aparte-touros` do JMP); as funções `fetchContent`, `youtubeEmbed`, `youtubeLink`.
  </action>
  <verify>
    <automated>grep -F -c "backgroundUrl: '/foto-leilao-eao.jpeg'" src/content.ts && grep -F -c "badge: 'VAGAS ABERTAS · 09 A 12 DE JULHO'" src/content.ts && grep -F -c "headline: 'Habilite-se para o\nMega Evento EAO\ncom a Bula.'" src/content.ts && grep -F -c "Seleção EAO avaliada animal por animal" src/content.ts && grep -F -c "value: '09–12 JUL'" src/content.ts && grep -F -c "locationLine1: 'Itagibá / BA'" src/content.ts && grep -F -c "locationLine2: 'Fazenda Baviera'" src/content.ts</automated>
  </verify>
  <done>
    - `DEFAULT_CONTENT.hero.backgroundUrl` é `/foto-leilao-eao.jpeg`
    - `badge`, `headline`, `valueProp`, `benefitsTitle` mencionam o Mega Evento EAO / Genética EAO, sem "JMP"
    - `benefits` tem exatamente os 5 itens especificados, na ordem especificada
    - `stats` tem exatamente 1 item (`09–12 JUL` / `2026 · 4 dias`), não 2
    - `locationLine1` é `Itagibá / BA` e `locationLine2` é `Fazenda Baviera`
    - `whatsappGroupUrl` e `blocks` de `DEFAULT_CONTENT` permanecem inalterados
    - `npx tsc --noEmit -p tsconfig.app.json` sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar identidade, título do form e info do evento em Form.tsx</name>
  <files>src/components/Form.tsx</files>
  <read_first>
    - src/components/Form.tsx — ler a linha 15 (import de `jmpLogo`), o bloco "Bula + JMP identity" (linhas ~418–435), o bloco de título do form dentro de `<div className="mb-7">` (linhas ~508–514), e o bloco "Event quick info" (linhas ~755–766). Confirmar que `tsconfig.app.json` tem `noUnusedLocals: true` — deixar o import `jmpLogo` sem uso quebra `tsc`.
  </read_first>
  <action>
    Quatro edições, todas dentro de `src/components/Form.tsx`, nenhuma outra:

    1. IMPORT — remover a linha `import jmpLogo from '../assets/jmp-logo.png'` por completo (linha 15). Manter o import de `bulaLogo` (linha 14) intocado.

    2. IDENTIDADE (bloco "Bula + JMP identity", dentro de `<div className="mb-10 flex items-center gap-5">`) — manter o `<img src={bulaLogo} .../>` e o divisor `<div className="h-10 w-px bg-white/30 sm:h-12" />` exatamente como estão. Substituir o segundo `<img src={jmpLogo} alt="JMP" .../>` por um wordmark de texto: um `<span>` com `className="text-white font-black text-xl sm:text-2xl uppercase tracking-tight leading-none"` contendo o texto literal `EAO BAVIERA`. Não é necessário nenhum asset novo — não existe logo EAO disponível, por isso o texto estilizado substitui a imagem. O alinhamento vertical continua garantido pelo `items-center` já presente no `div` pai.

    APÓS essa troca, confirme que NENHUM traço textual do logo antigo sobra dentro deste bloco de identidade: nem `jmpLogo` (import ou uso), nem o atributo `alt="JMP"`, nem `src={jmpLogo}`. Opcionalmente, atualize o comentário JSX `{/* Bula + JMP identity */}` (imediatamente acima do bloco) para `{/* Bula + EAO Baviera identity */}` — puramente cosmético, não afeta build nem verify.

    IMPORTANTE — não remover nenhuma OUTRA ocorrência de "jmp"/"JMP" existente no arquivo fora deste bloco de identidade: `import type { JmpHero } from '../content'` (linha 4) e `Form({ hero }: { hero: JmpHero })` (linha 323) são nomes de tipo TypeScript vindos de `content.ts`, não copy visível — não renomear; `const UTM_STORAGE_KEY = 'jmp_utm'` é uma chave interna de `sessionStorage`, sem relação com a marca — não alterar; o comentário `// Mesma origem da landing (jmp.bulaassessoria.com)...` e o endpoint `fetch('/api/jmp/lead', ...)` dentro de `submitForm` são infraestrutura de backend travada pelo escopo original desta fase — não tocar, sob nenhuma hipótese.

    3. TÍTULO DO FORM (dentro de `<div className="mb-7">`, no `<h2>` com classes `text-white font-black text-2xl sm:text-3xl leading-tight`) — trocar o conteúdo do `<span className="text-white/60 font-bold">` de `no JMP 2026` para `no Mega Evento EAO`. Manter a primeira linha `Garanta sua vaga<br />` e o `<p>` acima ("Grátis · Sem compromisso") inalterados.

    4. INFO RÁPIDA DO EVENTO (bloco "Event quick info", `<div className="mt-5 flex items-center justify-center gap-4 text-white/25 text-[11px]">`) — trocar o texto após o ícone `Calendar` de `14 Jun · Domingo · 09h` para `09 a 12 Jul · Fazenda Baviera`; trocar o texto após o ícone `MapPin` de `Campo Grande/MS` para `Itagibá / BA` (mesma grafia usada em `hero.locationLine1`). Manter os ícones `Calendar`/`MapPin`, os wrappers `<span className="inline-flex items-center gap-1.5">` e o divisor `<span className="w-px h-3 bg-white/15" />` inalterados.

    Não alterar, sob nenhuma hipótese: os bodies de Step 1 (Nome/E-mail/WhatsApp), Step 2 (UF/Cidade), Step 3 (Momento na Pecuária/Cabeças/Inscrição Estadual/Interesse/Quantidade), `validateStep`, `submitForm`, `onSubmit`, `handleChange`, `handleUFChange`, `goTo`, `UFCombobox`, captura de UTM (`captureUtms`), chamadas de analytics (`trackForm*`), o bloco "Trust signal", ou qualquer classe/estilo fora das quatro edições acima.
  </action>
  <verify>
    <automated>test $(grep -c "jmpLogo" src/components/Form.tsx) -eq 0 && test $(grep -Fc 'alt="JMP"' src/components/Form.tsx) -eq 0 && test $(grep -Fc "no JMP 2026" src/components/Form.tsx) -eq 0 && grep -F -c "EAO BAVIERA" src/components/Form.tsx && grep -F -c "no Mega Evento EAO" src/components/Form.tsx && grep -F -c "09 a 12 Jul · Fazenda Baviera" src/components/Form.tsx && grep -F -c "Itagibá / BA" src/components/Form.tsx && grep -F -c "/api/jmp/lead" src/components/Form.tsx && grep -F -c "JmpHero" src/components/Form.tsx && grep -F -c "jmp_utm" src/components/Form.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>
    - `jmpLogo`, `alt="JMP"` e `no JMP 2026` não aparecem mais em `Form.tsx` (zero ocorrências, checado explicitamente, não só o nome do import)
    - O bloco de identidade exibe o wordmark `EAO BAVIERA` ao lado do logo Bula
    - O título do form contém `no Mega Evento EAO`
    - O bloco "Event quick info" contém `09 a 12 Jul · Fazenda Baviera` e `Itagibá / BA`
    - `/api/jmp/lead`, `JmpHero` (import e prop) e `jmp_utm` continuam presentes e intactos — nenhuma infraestrutura de backend/tipo/storage foi tocada
    - Nenhuma linha de Step 1/2/3, `validateStep`, `submitForm`, `onSubmit`, `handleChange`, `handleUFChange`, `goTo` ou `UFCombobox` foi tocada
    - `npx tsc --noEmit -p tsconfig.app.json` sem erros
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar preload LCP e título em index.html; verificação final de typecheck e build</name>
  <files>index.html</files>
  <read_first>
    - index.html — arquivo completo (47 linhas). O preload LCP está na linha ~20 (`<link rel="preload" as="image" href="/foto-bulinha-bg.jpeg" fetchpriority="high" />`). O `<title>` está na linha ~36 (`<title>Nelore JMP — Inscrições</title>`).
  </read_first>
  <action>
    1. PRELOAD LCP — trocar `href="/foto-bulinha-bg.jpeg"` por `href="/foto-leilao-eao.jpeg"` no `<link rel="preload" as="image" ... fetchpriority="high" />`. Este valor DEVE ser idêntico ao `backgroundUrl` definido em `src/content.ts` na Task 1 (`/foto-leilao-eao.jpeg`) — caso contrário o navegador pré-carrega uma imagem diferente da que realmente é usada como fundo do hero, anulando o ganho de LCP. Manter `fetchpriority="high"` e o comentário acima do link inalterados.

    2. TÍTULO — trocar `<title>Nelore JMP — Inscrições</title>` por `<title>13º Mega Evento EAO Baviera — Inscrições</title>`.

    Não alterar: GTM script/noscript, favicons, viewport, preconnects e preload da fonte Inter, `<div id="root">`, script `src/main.tsx`.

    3. VERIFICAÇÃO FINAL — depois das duas edições acima (e já com as Tasks 1 e 2 aplicadas), rodar o typecheck explícito do app e o build completo de produção para confirmar que a remoção do import `jmpLogo`, o novo wordmark, e as trocas de copy em `content.ts`/`Form.tsx`/`index.html` compilam e buildam sem erro.
  </action>
  <verify>
    <automated>grep -F -c "/foto-leilao-eao.jpeg" index.html && grep -F -c "<title>13º Mega Evento EAO Baviera — Inscrições</title>" index.html && npx tsc --noEmit -p tsconfig.app.json && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>
    - O `<link rel="preload" as="image" ...>` aponta para `/foto-leilao-eao.jpeg` (idêntico ao `backgroundUrl` de `src/content.ts`)
    - `<title>` é `13º Mega Evento EAO Baviera — Inscrições`
    - `npx tsc --noEmit -p tsconfig.app.json` sai com código 0
    - `npm run build` conclui com exit 0 (tsc -b + vite build), bundle gerado em `dist/`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Mudança puramente de conteúdo estático (strings de copy, um asset de imagem já versionado, um wordmark de texto). Sem inputs de usuário novos, sem chamadas de API novas, sem dados sensíveis. O formulário e seu POST para `/api/jmp/lead` não são tocados neste plano. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | src/content.ts (DEFAULT_CONTENT.hero) | accept | Conteúdo estático hardcoded no bundle, sem input externo; `fetchContent()` (não alterado) ainda pode sobrescrever em runtime via `/api/jmp/content`, comportamento pré-existente e fora de escopo |
| T-03-01-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
- `grep -F "backgroundUrl: '/foto-leilao-eao.jpeg'" src/content.ts` retorna 1 match
- `grep -F "value: '09–12 JUL'" src/content.ts` retorna 1 match (array `stats` com 1 único item)
- `test $(grep -c "jmpLogo" src/components/Form.tsx) -eq 0` (import e uso removidos)
- `grep -F "EAO BAVIERA" src/components/Form.tsx` retorna 1 match
- `grep -F "no Mega Evento EAO" src/components/Form.tsx` retorna 1 match
- `grep -F "/foto-leilao-eao.jpeg" index.html` retorna 1 match (preload LCP alinhado ao `backgroundUrl`)
- `grep -F "13º Mega Evento EAO Baviera — Inscrições" index.html` retorna 1 match
- `npx tsc --noEmit -p tsconfig.app.json` sem erros
- `npm run build` conclui com exit 0
</verification>

<success_criteria>
A landing page exibe integralmente o conteúdo e a identidade do 13º Mega Evento EAO Baviera (badge, headline, benefícios, stat de data, localização, wordmark, título do form e info rápida do evento) em vez do leilão Nelore JMP, com o asset de background alinhado entre `src/content.ts` e o preload LCP de `index.html`. O formulário de captura de leads (campos, steps, validação, submit para `/api/jmp/lead`) permanece byte-idêntico ao que já está em produção. `npx tsc --noEmit -p tsconfig.app.json` e `npm run build` passam limpos.
</success_criteria>

<output>
Criar `.planning/phases/03-mega-evento-eao-baviera/03-01-SUMMARY.md` ao concluir.
</output>
