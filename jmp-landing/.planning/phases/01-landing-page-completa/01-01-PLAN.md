---
phase: 01-landing-page-completa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tailwind.config.js
  - postcss.config.js
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
autonomous: true
requirements:
  - TECH-01
  - TECH-02
  - VIS-02
must_haves:
  truths:
    - "npm run dev starts without errors"
    - "Tailwind CSS classes apply to elements"
    - "Inter font loads from Google Fonts"
  artifacts:
    - path: "package.json"
      provides: "Vite + React 18 + Tailwind CSS v3 + lucide-react dependencies"
      contains: "\"react\":"
    - path: "src/App.tsx"
      provides: "Root component shell with placeholder sections"
    - path: "tailwind.config.js"
      provides: "Tailwind config with custom palette"
      contains: "C8A96E"
    - path: "src/index.css"
      provides: "Tailwind directives + Inter font import"
      contains: "@tailwind"
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "script module import"
      pattern: "src/main"
    - from: "src/index.css"
      to: "Inter font"
      via: "Google Fonts import"
      pattern: "fonts.googleapis.com.*Inter"
---

<objective>
Scaffold the Vite + React 18 + Tailwind CSS v3 project with the Bula Assessoria color palette and Inter font.

Purpose: Creates the foundation that all other plans build on. Must produce a running dev server before any components are built.
Output: Working Vite project with Tailwind configured, Inter font loaded, and App.tsx skeleton ready to receive components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React 18 project and install dependencies</name>
  <files>package.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx</files>
  <read_first>package.json</read_first>
  <action>
    Run: npm create vite@latest . -- --template react-ts (answer "y" to overwrite if prompted)
    Then install dependencies:
      npm install
      npm install -D tailwindcss@3 postcss autoprefixer
      npm install lucide-react
    
    Initialize Tailwind:
      npx tailwindcss init -p
    
    This creates: package.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/App.css, src/index.css, src/assets/
  </action>
  <verify>ls package.json vite.config.ts tailwind.config.js postcss.config.js</verify>
  <acceptance_criteria>
    - package.json contains "react": "^18"
    - package.json contains "tailwindcss"
    - package.json contains "lucide-react"
    - tailwind.config.js exists
    - postcss.config.js exists
  </acceptance_criteria>
  <done>Vite project scaffolded with all dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind with Bula palette and Inter font</name>
  <files>tailwind.config.js, src/index.css, index.html</files>
  <read_first>tailwind.config.js, src/index.css, index.html</read_first>
  <action>
    Update tailwind.config.js to:
    ```js
    /** @type {import('tailwindcss').Config} */
    export default {
      content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
      theme: {
        extend: {
          colors: {
            gold: { DEFAULT: '#C8A96E', dark: '#A68B4B' },
            whatsapp: '#25D366',
          },
          fontFamily: {
            sans: ['Inter', 'sans-serif'],
          },
        },
      },
      plugins: [],
    }
    ```

    Replace src/index.css entirely with:
    ```css
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap');

    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    * {
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }
    ```

    Update index.html: add lang="pt-BR" to <html>, update <title> to "Nelore JMP — Inscrições".
    Also add to <head>: <link rel="preconnect" href="https://fonts.googleapis.com"> and <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  </action>
  <verify>grep -r "C8A96E" tailwind.config.js && grep -r "fonts.googleapis.com" src/index.css</verify>
  <acceptance_criteria>
    - tailwind.config.js contains "C8A96E"
    - tailwind.config.js content array includes "./src/**/*.{js,ts,jsx,tsx}"
    - src/index.css contains "@tailwind base"
    - src/index.css contains "fonts.googleapis.com"
    - src/index.css contains "Inter"
    - index.html title is "Nelore JMP — Inscrições"
  </acceptance_criteria>
  <done>Tailwind configured with Bula palette and Inter font imported</done>
</task>

<task type="auto">
  <name>Task 3: Create App.tsx skeleton and WhatsApp constant</name>
  <files>src/App.tsx, src/constants.ts</files>
  <read_first>src/App.tsx</read_first>
  <action>
    Create src/constants.ts:
    ```ts
    // WhatsApp group link — update before deploy
    export const WHATSAPP_GROUP_URL = 'https://chat.whatsapp.com/PLACEHOLDER_LINK_AQUI'
    ```

    Replace src/App.tsx with a skeleton that imports all 4 future components as TODO placeholders:
    ```tsx
    import './index.css'

    function App() {
      return (
        <div className="min-h-screen font-sans bg-white text-black">
          {/* Header — Plan 02 */}
          {/* Hero — Plan 02 */}
          {/* Form — Plan 03 */}
          {/* LeilaoCard — Plan 04 */}
          {/* Footer — Plan 05 */}
          <div className="flex items-center justify-center h-screen">
            <p className="text-gold font-bold text-2xl">Nelore JMP — Em construção</p>
          </div>
        </div>
      )
    }

    export default App
    ```

    Delete src/App.css (not needed, Tailwind replaces it).
    In src/main.tsx ensure it imports './index.css' (not './App.css').
  </action>
  <verify>node -e "require('./src/constants.ts')" 2>/dev/null || grep "WHATSAPP_GROUP_URL" src/constants.ts</verify>
  <acceptance_criteria>
    - src/constants.ts contains "WHATSAPP_GROUP_URL"
    - src/constants.ts contains "PLACEHOLDER_LINK_AQUI"
    - src/App.tsx imports "./index.css"
    - src/App.tsx contains "font-sans"
    - src/main.tsx imports "./index.css"
  </acceptance_criteria>
  <done>App.tsx skeleton created with WhatsApp constant in constants.ts</done>
</task>

<task type="auto">
  <name>Task 4: Verify dev server starts</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run: npm run build
    This validates the TypeScript compiles and Tailwind processes correctly without starting a long-running server.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - npm run build exits with code 0
    - No TypeScript errors in output
  </acceptance_criteria>
  <done>Project builds successfully</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds (exit 0)
- [ ] tailwind.config.js contains custom colors (C8A96E, 25D366)
- [ ] src/index.css imports Inter from Google Fonts
- [ ] src/constants.ts exports WHATSAPP_GROUP_URL
</verification>

<success_criteria>
- All tasks completed
- npm run build succeeds without errors
- Tailwind palette and Inter font configured
- WHATSAPP_GROUP_URL constant in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page-completa/01-01-SUMMARY.md`
</output>
