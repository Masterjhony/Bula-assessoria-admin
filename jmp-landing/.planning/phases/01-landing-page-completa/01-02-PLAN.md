---
phase: 01-landing-page-completa
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/components/Header.tsx
  - src/components/Hero.tsx
autonomous: true
requirements:
  - LAY-01
  - LAY-02
  - VIS-01
  - VIS-03
  - VIS-04
must_haves:
  truths:
    - "Header is fixed at top with Bula logo and WhatsApp button"
    - "Hero section has background image with dark overlay"
    - "Header WhatsApp button links to WHATSAPP_GROUP_URL"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Fixed header with logo and WhatsApp CTA"
      min_lines: 20
      contains: "fixed"
    - path: "src/components/Hero.tsx"
      provides: "Hero section with background image overlay"
      min_lines: 20
      contains: "bg-cover"
  key_links:
    - from: "src/components/Header.tsx"
      to: "src/constants.ts"
      via: "WHATSAPP_GROUP_URL import"
      pattern: "WHATSAPP_GROUP_URL"
    - from: "src/components/Hero.tsx"
      to: "foto_bulinha-background.jpeg"
      via: "backgroundImage style or Tailwind bg-[url()]"
      pattern: "bulinha-background|foto_bulinha"
---

<objective>
Build the fixed Header component with Bula logo and WhatsApp CTA button, and the Hero section with the bulinha background image and dark overlay.

Purpose: Top-of-page visual identity and primary conversion entry point.
Output: Two standalone components (Header.tsx, Hero.tsx) ready to be assembled into App.tsx by Plan 05.
NOTE: Do NOT modify App.tsx — Plan 05 handles final assembly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-landing-page-completa/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component with fixed positioning and WhatsApp button</name>
  <files>src/components/Header.tsx</files>
  <read_first>src/constants.ts, tailwind.config.js</read_first>
  <action>
    Create src/components/Header.tsx:

    ```tsx
    import { MessageCircle } from 'lucide-react'
    import { WHATSAPP_GROUP_URL } from '../constants'
    import logo from '../../logo_bula.PNG'

    export function Header() {
      return (
        <header className="fixed top-0 left-0 right-0 z-50 bg-black/90 backdrop-blur-sm border-b border-white/10">
          <div className="max-w-6xl mx-auto px-4 sm:px-6 h-16 flex items-center justify-between">
            <img
              src={logo}
              alt="Bula Assessoria Pecuária"
              className="h-10 w-auto object-contain"
            />
            <a
              href={WHATSAPP_GROUP_URL}
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center gap-2 bg-whatsapp hover:bg-whatsapp/90 text-white font-semibold text-sm px-4 py-2 rounded-md transition-all duration-200 hover:-translate-y-0.5 hover:shadow-lg"
            >
              <MessageCircle className="w-4 h-4" />
              WhatsApp
            </a>
          </div>
        </header>
      )
    }
    ```

    Note: logo_bula.PNG is at the project root. The relative import path from src/components/ is ../../logo_bula.PNG.
    If Vite can't resolve PNG from root directly, copy it to src/assets/logo_bula.PNG and update the import accordingly — prefer src/assets/ path for cleaner imports.
  </action>
  <verify>grep -l "WHATSAPP_GROUP_URL" src/components/Header.tsx && grep "fixed" src/components/Header.tsx</verify>
  <acceptance_criteria>
    - src/components/Header.tsx contains "fixed"
    - src/components/Header.tsx contains "WHATSAPP_GROUP_URL"
    - src/components/Header.tsx contains "MessageCircle"
    - src/components/Header.tsx contains "z-50"
    - src/components/Header.tsx contains "backdrop-blur"
    - src/components/Header.tsx imports logo from an existing path (../../logo_bula.PNG or ../../src/assets/logo_bula.PNG)
  </acceptance_criteria>
  <done>Header fixed at top with logo and WhatsApp button</done>
</task>

<task type="auto">
  <name>Task 2: Copy logo asset to src/assets and verify import path</name>
  <files>src/assets/logo_bula.PNG</files>
  <read_first>src/components/Header.tsx</read_first>
  <action>
    Copy logo_bula.PNG to src/assets/ so Vite can process it cleanly:
    cp logo_bula.PNG src/assets/logo_bula.PNG

    Also copy the hero background image:
    cp foto_bulinha-background.jpeg src/assets/foto_bulinha-background.jpeg

    Update Header.tsx import path to: import logo from '../assets/logo_bula.PNG'
    (since Header.tsx is in src/components/, src/assets is one level up via ../assets)
  </action>
  <verify>ls src/assets/logo_bula.PNG src/assets/foto_bulinha-background.jpeg</verify>
  <acceptance_criteria>
    - src/assets/logo_bula.PNG exists
    - src/assets/foto_bulinha-background.jpeg exists
    - src/components/Header.tsx imports from "../assets/logo_bula.PNG"
  </acceptance_criteria>
  <done>Assets in src/assets, import path updated</done>
</task>

<task type="auto">
  <name>Task 3: Create Hero section with background image and dark overlay</name>
  <files>src/components/Hero.tsx</files>
  <read_first>tailwind.config.js, src/assets/foto_bulinha-background.jpeg</read_first>
  <action>
    Create src/components/Hero.tsx:

    ```tsx
    import heroImg from '../assets/foto_bulinha-background.jpeg'

    export function Hero() {
      return (
        <section
          className="relative min-h-[60vh] flex items-center justify-center pt-16"
          style={{
            backgroundImage: `url(${heroImg})`,
            backgroundSize: 'cover',
            backgroundPosition: 'center',
          }}
        >
          {/* Dark overlay */}
          <div className="absolute inset-0 bg-black/60" />

          <div className="relative z-10 text-center text-white px-4 py-16 max-w-3xl mx-auto">
            <p className="text-gold font-semibold text-sm uppercase tracking-widest mb-3">
              Nelore JMP · Criatório de Elite
            </p>
            <h1 className="text-4xl sm:text-5xl font-black leading-tight mb-4">
              Inscreva-se no<br />
              <span className="text-gold">Leilão JMP 2025</span>
            </h1>
            <p className="text-white/80 text-lg max-w-xl mx-auto">
              Touros, Matrizes, Embrião e Sêmen de alta genética.
              Preencha o formulário e garanta sua participação.
            </p>
          </div>
        </section>
      )
    }
    ```
  </action>
  <verify>grep "bg-black/60" src/components/Hero.tsx && grep "heroImg" src/components/Hero.tsx</verify>
  <acceptance_criteria>
    - src/components/Hero.tsx imports heroImg from "../assets/foto_bulinha-background.jpeg"
    - src/components/Hero.tsx contains "bg-black/60" (dark overlay)
    - src/components/Hero.tsx contains "text-gold"
    - src/components/Hero.tsx contains "font-black"
  </acceptance_criteria>
  <done>Hero section with background image and dark overlay built</done>
</task>

<task type="auto">
  <name>Task 4: Build check</name>
  <files></files>
  <read_first></read_first>
  <action>Run npm run build to verify Header and Hero compile without TypeScript errors.</action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - npm run build exits successfully (no TypeScript errors from Header.tsx or Hero.tsx)
  </acceptance_criteria>
  <done>Components build without errors</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] src/components/Header.tsx exists with fixed positioning and WhatsApp link
- [ ] src/components/Hero.tsx exists with background image
- [ ] Assets copied to src/assets/
</verification>

<success_criteria>
- Header and Hero components built as standalone files
- App.tsx NOT modified (Plan 05 handles assembly)
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page-completa/01-02-SUMMARY.md`
</output>
