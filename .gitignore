node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env.*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.DS_Store
Thumbs.db
*.pem
.vscode/
.idea/

next-env.d.ts
tsconfig.tsbuildinfo

.vercel
qa-screenshots/

# ─────────────────────────────────────────────────────────────────────
# Dados internos e artefatos gerados — NÃO devem ir para o remoto.
# Contêm dados financeiros/comerciais reais (faturamentos, comissões,
# clientes). O app só precisa de código; estes arquivos são insumos ou
# saídas locais de trabalho.
# ─────────────────────────────────────────────────────────────────────

# Planilhas e documentos internos
*.xlsx
*.docx

# Drops/zip de código-fonte (ex.: pagina_forms-JMP.zip). O conteúdo já foi
# integrado em jmp-landing/ + public/jmp/; o zip é só um insumo local.
*.zip

# Relatórios gerados (financeiro/fechamento) e a pasta de saída
/RELATORIO-*
/relatorios/

# Catálogos, contratos e mídias soltas na raiz (drops internos).
# Assets reais do app ficam versionados em public/ — por isso o ignore
# é ancorado na raiz (leading slash) e não atinge public/.
/*.pdf
/*.png
/*.jpg
/*.jpeg

# Anotações/prompts de desenvolvimento na raiz
/prompt-*.md

# Scripts de trabalho/scratch (convenção: prefixo "_") e seus dumps
scripts/_*
Bula_Assessoria_Leads_Instagram_2026-06-23_a_2026-06-24.xlsx

# Dados de conciliação com CPF/PII — não versionar
scripts/data/

# cache local da análise de conversas (IA)
outputs/.cache-conversas-ia.json

# Graphify — knowledge graph gerado (determinístico, rebuild via git hooks / `graphify update .`)
graphify-out/
