import { PlaceholderPage } from '@/components/admin/PlaceholderPage'

export default function Page() {
  return (
    <PlaceholderPage
      title="Central WhatsApp"
      phase="Fase 6"
      description="Será implementado na Fase 6 (UI + schema + APIs com stubs). O servidor Baileys será conectado depois — você criará servidor próprio."
    />
  )
}
